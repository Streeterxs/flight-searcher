import React from 'react';

import { Form } from './Components';

export const Landing = () => {
    
    return (
        <div>
            Landing Page!
            <Form/>
        </div>
    );
};
